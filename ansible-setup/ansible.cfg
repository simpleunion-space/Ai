[defaults]
# Несколько inventory живут под inventories/<name>/ - путь передаётся явно
# через -i при каждом запуске, здесь намеренно не зафиксирован дефолтный.
roles_path = roles
host_key_checking = True
retry_files_enabled = False

[privilege_escalation]
become = True
become_method = sudo
